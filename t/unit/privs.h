/* Minimal mock of ProFTPD's privs.h for the isolated unit test. */
#ifndef MOCK_PRIVS_H
#define MOCK_PRIVS_H

#define PRIVS_ROOT do { } while (0)
#define PRIVS_RELINQUISH do { } while (0)

#endif /* MOCK_PRIVS_H */
